# AX Festival 앱 · 작업 인수인계

기준: v4.49 (2026-09-25, main `0da20be`) · 행사 10월 26일 광화문 본사

이 문서는 코드만 봐서는 알 수 없는 것을 적는다. 서버 한계, 현장 운영 방식, 아직 정하지 않은 것, 작업 요령이다.
화면 디자인 규칙은 `design-handoff/`(README · DESIGN-CONTRACT · PAGES)를 따른다.

---

## 1. 구조 한눈에

| 파일 | 역할 |
|---|---|
| `index.html` | 참가자 앱 전체(단일 파일 PWA) · **CRLF 줄끝** |
| `admin/index.html` | 운영 콘솔 · LF 줄끝 |
| `sw.js` · `manifest.json` | PWA |
| `.claude/agents/redteam-senior.md` | 기획 레드팀(50대 고참 직원 페르소나) · 기획 방향이 고민될 때만 부른다 |

- 서버는 Google Apps Script(GAS) 웹앱이다. 앱은 `beCall()`(JSONP)로 부르고, 주기적인 `beSync()`(action `sync`)가 내 상태를 받아온다.
- 스탬프, 룰렛 사용 여부, 응모 번호, 상담·커피챗 상태는 **서버가 정본**이다. 앱은 받아서 쓴다.
- 로컬 저장은 `S.get` / `S.set`(localStorage, `axf_` 접두어)이다.
- 테스트
  - `index.html#demo=home`은 데모다. 서버 없이 화면을 볼 수 있다.
  - 테스트 사번 `310555`는 스탬프·룰렛을 로컬에만 기록한다.

## 2. 서버(GAS)는 이 저장소에 없다

GAS는 「[운영] AX페스티벌」 스프레드시트에 붙은 Apps Script다. 저장소 밖에 있어서 코드 작업 환경(Claude 포함)에서는 읽거나 고칠 수 없다.
그래서 서버를 고치지 않고 우회한 곳이 두 군데 있다.

### 2-1. 커피챗 희망 시간 (v4.46)
- 앱은 `cchat_req`의 `tag`를 `"<관심 분야> / 희망 <시간대>"` 형식으로 보낸다(`CCHAT_TAG_SEP`). 서버가 이미 저장하는 칸이다.
- 콘솔은 `ccSplitPref()`로 태그와 희망 시간을 다시 나눠 보여 준다.
- 한계: 시트를 직접 열면 「관심 태그」 칸에 합쳐진 글이 보인다.
- 제대로 고치려면 GAS에서 `cchat_req`의 `pref`를 저장하고 `cchat_list`와 `sync`에서 돌려주면 된다. 그러면 그 값이 우선한다.

### 2-2. 커피챗 신청 마감 신호 (v4.47)
- 콘솔 [신청 마감]은 제목이 `[운영] 커피챗 신청 마감`인 공지를 보낸다(`notice_set`).
  - 앱은 이 공지를 참가자에게 띄우지 않고 신청만 막는다.
  - [신청 다시 열기]는 그 공지를 중지한다(`notice_clear`).
- **한계:** 서버 공지는 한 번에 하나다.
  - 마감 중에 다른 공지를 보내면, 그 뒤에 앱을 처음 여는 사람에게는 마감이 전달되지 않는다. 이미 받은 기기는 마감 상태를 유지한다.
  - 콘솔이 발송 전에 경고를 띄운다.
- 제대로 고치려면 GAS에 `roulette_out`과 같은 모양의 스위치(`cchat_out`)를 만들고, `sync` 응답에 `cchatOut: true/false`를 넣으면 된다. 앱은 이미 이 값을 읽는다(`cchat_out`).

## 3. 현장 운영 · 직원 교육

### 보너스 룰렛 (v4.47)
- 커피챗 참석과 DAP 과제상담 참석마다 룰렛을 1회씩 더 준다(최대 2회). 기본 룰렛(스탬프 3개)과는 별개다.
- 열리는 기준은 서버 상태다.
  - 커피챗: `참석` 또는 `완료`
  - DAP: `체크인` 또는 `완료`
- **1F 룰렛 직원은 보너스를 QR로 스캔하면 안 된다.**
  - 서버 `roulette_redeem`은 1인 1회라, 스캔하면 기본 1회권이 차감된다.
  - 보너스는 참가자 앱 「내 보상 › 보너스 룰렛」 화면에서 직원이 **[직원 확인 · 사용 처리]를 두 번** 눌러 처리한다.
- 직원 스캐너가 「이미 룰렛을 돌린 참가자」라고 하면, 보너스는 참가자 화면에서 처리하라는 안내가 같이 나온다.
- 알려진 약점
  - 사용 기록이 그 휴대폰에만 남는다. 다른 기기로 로그인하면 한 번 더 쓸 수 있다. 룰렛 1회라 감수했다.
  - 입장 기준이라 18F 입장 스캔만 하고 나가도 열린다.

### 커피챗 정원
- 앱과 콘솔에 **선착순 30명**이 들어가 있다(`CCHAT_CAP` / `CC_CAP`).
- 서버는 인원을 세지 않는다. 운영자가 콘솔 커피챗 표의 「선착순 N / 30명」을 보고 수동으로 마감한다.
- 매칭은 운영자가 콘솔에서 회차와 테이블을 넣어 하고, 참가자에게는 하이웍스로 안내한다.

### 응모권 (v4.42~v4.49)
- 스탬프 4개부터 응모권이 생기면 보물상자 시트가 뜬다.
- v4.49부터는 **모든 응모권을 직접 열어야** 번호가 보인다.
- [나중에 보기]로 닫아도 응모는 이미 된 것이다. 안 연 번호는 스탬프 레일의 「새 번호 확인」 칩으로 다시 열 수 있다.

## 4. 리허설 때 실제 서버로 확인할 것

지금까지는 서버 응답을 흉내 낸 테스트(Playwright)로만 확인했다.

- [ ] 18F 커피챗 입장 스캔 뒤 `sync`의 `my.cchat.status`가 `참석`으로 내려오는지. 안 내려오면 보너스 룰렛이 열리지 않는다.
- [ ] DAP 체크인·완료 뒤 보너스 룰렛이 열리는지
- [ ] 콘솔 [신청 마감]이 참가자 앱에 약 10초 안에 반영되는지, [다시 열기]로 풀리는지
- [ ] 스탬프를 빠르게 모을 때, 응모권 상자를 매번 직접 여는 것이 번거롭지 않은지(v4.49 결정 재확인)
- [ ] 큰글씨 모드와 360px 폭 폰에서 하단 QR 버튼(v4.48, 흰 테두리 원 52px)이 어떻게 보이는지

## 5. 아직 정하지 않은 것 (화요일 회의 안건)

| 항목 | 지금 앱 값 | 고칠 곳 |
|---|---|---|
| 커피챗 정원 | 30명 | `index.html` `CCHAT_CAP` · `admin/index.html` `CC_CAP` |
| 회차 · 운영 시간 | 희망 시간대 13~17시 1시간 단위 4칸 + 「언제든」 | `index.html` `CCHAT_PREF` |
| 보너스 룰렛 열림 기준 | 입장(참석 · 체크인) | `index.html` `bonusRlOpen()` · sync 의 `cchat_att` |
| 보너스 방식 | 커피챗 · DAP 각 1회(최대 2회) | `index.html` `BONUS_RL` · `bonusRlOpen()` |
| GAS 마감 스위치 추가 | 공지 신호로 우회 중 | GAS (2-2 참고) |

- 멘토 2명이 각자 테이블에서 4명씩 6회차를 하면 48석이라 정원 30명과 맞지 않는다. 4회차(32석)면 충분하다.
- 보너스를 각 1회로 주면 룰렛 회전수가 늘어난다. 경품 수량과 함께 결정해야 한다. 재고가 모자라면 콘솔 「룰렛 소진 안내」를 켜야 한다.

## 6. 작업 요령

- **`index.html`은 CRLF다.** LF로 바꿔 저장하는 편집기나 스크립트를 쓰면 변경 내역 전체가 바뀐다. 파이썬으로 고칠 때는 바이트로 읽고 `\r\n`을 유지한다.
- 수정마다 주석에 `/* vX.YY (사용자 날짜) 이유 */`를 남기는 관례를 따른다. 커밋 제목도 `vX.YY 요약`이다.
- 스크립트 문법 검사 방법: `<script>` 본문을 뽑아 `node --check`로 확인한다.
- 화면 확인은 Playwright로 `index.html#demo=home`을 열어 스크린샷을 찍는다. 서버 응답은 `page.route('https://script.google.com/**', …)`로 흉내 낸다.
- 파랑 색은 게임판 안에서만 쓴다(사용자 결정). 나머지는 AX-TDS 토큰을 쓴다.
- 17F 강연은 신청을 받지 않는다(자유 참석, 대강당 입구 QR 출석).
- 커피챗은 아이디어 한 줄 제출이 필수다.
